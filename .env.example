# ==============================================
# BACKEND ENVIRONMENT VARIABLES
# ==============================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit your .env file!
# ==============================================

# ==============================================
# REQUIRED - App will fail to start without these
# ==============================================

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority

# JWT Authentication
# CRITICAL: Generate a secure key for production!
# Run: openssl rand -base64 32
JWT_SECRET_KEY=

# AI API Keys
OPENAI_API_KEY=sk-your-openai-api-key
DEEPSEEK_API_KEY=sk-your-deepseek-api-key

# Cloudflare R2 Storage (S3-compatible)
# Get these from Cloudflare Dashboard > R2 > Manage R2 API Tokens
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_ENDPOINT=https://<account_id>.r2.cloudflarestorage.com
R2_BUCKET_NAME=your-bucket-name

# ==============================================
# OPTIONAL - Have sensible defaults
# ==============================================

# Environment (development or production)
ENVIRONMENT=development

# Database name
MONGODB_DATABASE=docurag

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173
APP_URL=http://localhost:5173

# JWT Settings
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# Cookie Settings (for production HTTPS)
COOKIE_SECURE=false
COOKIE_DOMAIN=

# Storage Provider (r2 or s3)
STORAGE_PROVIDER=r2

# ==============================================
# GOOGLE OAUTH (Optional)
# ==============================================
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/api/auth/google/callback

# ==============================================
# EMAIL SERVICE (Optional - Resend)
# ==============================================
RESEND_API_KEY=
FROM_EMAIL=Querious <onboarding@resend.dev>
APP_NAME=Querious

# ==============================================
# AWS S3 (Alternative to R2 - Optional)
# ==============================================
# Set STORAGE_PROVIDER=s3 to use AWS S3 instead of R2
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET=
